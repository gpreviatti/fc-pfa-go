apiVersion: apps/v1
kind: Deployment
metadata:
  name: producer-deploy
  namespace: pfa-go
spec:
  replicas: 1
  selector:
    matchLabels:
      app: producer-deploy
  template:
    metadata:
      labels:
        app: producer-deploy
    spec:
      containers:
      - name: goserver
        image: giov3nn10/pfa-go:producer
        env:
          - name: ENVIRONMENT
            value: Production
